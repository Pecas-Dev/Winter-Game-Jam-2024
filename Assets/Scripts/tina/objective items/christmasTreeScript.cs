using UnityEngine;

public class christmasTreeScript : MonoBehaviour
{
    // add to the parent gameobject
    // check if inOriginalState == false from interactableObject(), then do smth

    

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
